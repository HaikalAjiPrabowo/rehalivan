import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'learning-course-marketplace',
  webDir: 'www'
};

export default config;
